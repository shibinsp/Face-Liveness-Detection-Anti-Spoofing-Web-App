# Core dependencies
streamlit>=1.20.0
opencv-python>=4.7.0
numpy>=1.26.0,<2.0.0  # TensorFlow 2.16 requires numpy<2
pandas>=2.0.0
mediapipe==0.10.13  # Stable version compatible with TensorFlow 2.16

# Anti-spoofing package (recommended)
anti-spoof>=0.1.0

# For InsightFace (full version)
insightface>=0.7.0
onnxruntime>=1.14.0

# Optional: For GPU acceleration
# Uncomment the line below and comment out onnxruntime above
# onnxruntime-gpu>=1.14.0

# Additional dependencies for full functionality
scipy>=1.10.0
scikit-learn>=1.2.0
scikit-image>=0.20.0
matplotlib>=3.7.0
Pillow>=9.5.0

# Note: InsightFace requires Microsoft Visual C++ Build Tools on Windows
# Download from: https://visualstudio.microsoft.com/visual-cpp-build-tools/

# Face Recognition & Authentication
deepface>=0.0.79
ultralytics>=8.0.0  # YOLO v11
tensorflow>=2.15.0,<2.17.0  # Compatible with MediaPipe's protobuf requirements
tf-keras~=2.16.0  # Required for TensorFlow 2.16 + Keras 3.x compatibility
protobuf>=4.25.3,<5  # Required by MediaPipe
keras>=3.0.0

# Important Notes:
# 1. Do NOT install jax or jaxlib - conflicts with ml_dtypes requirements
# 2. tf-keras must match TensorFlow version (2.16.x for TF 2.16.x)
# 3. Install tf-keras with --no-deps to prevent TensorFlow upgrade
